// não vai ser um codigo executável!!!

// Procedural
procesimento(valor1, valor2, valor3)

// OO
Objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {

    }
}

objeto.processamento() // foco passou a ser o objeto.

// Principios importantes:
// 1 = abstração.... é pegar um objeto real e abstrair para o sistema
// 2 = encapsulamento = que todas sa informações fique dentro do sistema encapsuladas e nao transpareça para interface final
// 3 = herança (prototype) ... é uma forma de reusar codigo. recebe atributos de um objeto "pai"
// 4 polimorfismo = significa que tem varias formas.